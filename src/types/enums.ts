export enum CustomErrors {
  SERVER_ERROR = 'Sorry, there is error with server response',
  ALREADY_REGISTER_ERROR = 'There is already an existing customer with the provided email.',
  LOGIN_ERROR = 'There is no an existing customer with the provided email.',
  PASSWORD_ERROR = 'You entered the wrong password.',
}
